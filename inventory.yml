---
all:
  children:
    nas:
      hosts:
        nascsch.cklan:

    vpn:
      hosts:
        nascsch.cklan:
        ckaccess.cklan:

    dns:
      hosts:
        nascsch.cklan:
        ckaccess.cklan:
    hassio:
      hosts:
        hassio.cklan:
