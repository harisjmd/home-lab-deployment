---
all:
  children:
    nas:
      hosts:
        nascsch.cklan:
        
    access:
      hosts:
        nascsch.cklan:
        ckaccess.cklan:

    main:
      hosts:
        hassio.cklan:
      children:
        nas:

    backup:
      hosts:
        ckaccess.cklan:
